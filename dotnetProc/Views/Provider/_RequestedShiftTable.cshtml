@model List<HalloDoc_DAL.ProviderViewModels.RequestedShiftDetails>


@{

    string[] status = { "Active", "Pending", "deactive" };
    <form id="RequestShiftTableForm" asp-action="ApproveShiftAction" asp-controller="Provider" method="post">


        <input type="number" hidden name="Val" id="ApproveInp" style="display:none;" />
        


        <table class="table ReqtableContainer table-responsive">
            <thead>
                <tr style="background-color:#f1f1f1;">
                    <th class="reqtableHeader" scope="col"><input class="form-check-input" type="checkbox" onclick="checkAll('shiftCheckBox',event.target)"></th>
                    <th class="reqtableHeader" scope="col">Staff <span>&uarr;</span></th>
                    <th class="reqtableHeader" scope="col">Day</th>
                    <th class="reqtableHeader" scope="col">Time</th>
                    <th class="reqtableHeader" scope="col">Region</th>
                    
                </tr>
            </thead>
            <tbody style="color: var(--pi-label-color);background: var(--pi-form-back-color);">

                @for(int i = 0; i < Model.Count; i++)
                {
                    <tr>

                        <input  asp-for="@Model[i].ShiftDetailId" style="display:none;" hidden />
                        <th><input asp-for="@Model[i].IsSelected" class="form-check-input shiftCheckBox"  type="checkbox"> </th>
                        <th>@Model[i].PhysicianName</th>
                        <th>@Model[i].Day.ToString("MMM d,yyyy")</th>
                        <th>@Model[i].StartTime.ToString("h:mm tt") - @Model[i].EndTime.ToString("h:mm tt")</th>
                        <th>@Model[i].Region</th>
                    </tr>
                }

            </tbody>

        </table>



        <ul id="mb-ViewUploadTable" style="border-radius:0px 0px 10px 10px;" class="list-group">
            @for (int i = 0; i < Model.Count; i++)

            {

                <li class="list-group-item" style="background-color:var(--pi-form-back-color);color:var(--pi-label-color);">
                    <div class="container-fluid" style="row-gap:1rem;">
                        <div class="row mb-3">

                            <div class="col-2">
                                <input asp-for="@Model[i].IsSelected" class="form-check-input shiftCheckBox" type="checkbox">
                            </div>
                            <div  class="col-8" style="padding:0rem;">

                                @Model[i].PhysicianName

                            </div>
                        </div>
                        <div class="row mb-3">

                            <div class="col-12">
                                @Model[i].Day.ToString("MMM d,yyyy")
                            </div>

                        </div> 
                        <div class="row mb-3">

                            <div class="col-12">
                                @Model[i].StartTime.ToString("h:mm tt") - @Model[i].EndTime.ToString("h:mm tt")
                            </div>

                        </div>
                        <div class="row mb-3">

                            <div class="col-12">
                                @Model[i].Region
                            </div>

                        </div>
                       

                    </div>
                </li>
             
            }
        </ul>




    </form>

}


<script>

    //$("document").ready(function () {

    //    DisPlayPagination();

    //})


    const handleNotificationChange = () => {

        if ($("#ProviderChangeSaveBtn").css("display") == "none") {
            $("#ProviderChangeSaveBtn").css("display", "block");
        }
    }


     


</script>